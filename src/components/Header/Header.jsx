import "./Header.css"

const Header = () => {
   
    return (
        <nav className="header"> 
            <a href="#">React</a>
            <a href="#">React</a>
            <a href="#">React</a>
        </nav>
    );
};

export default Header;